<% @items.each do |item| %>
  category: <%= item.category.name %>, description: '<%= item.description %>', date: '<%= item.date %>', created_at: '<%= item.created_at %>', updated_at: '<%= item.updated_at %>', money: <%= item.money %>
<% end %>